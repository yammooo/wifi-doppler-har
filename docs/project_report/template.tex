\documentclass[10pt,conference,letterpaper]{IEEEtran}

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{cite}
\usepackage{graphicx}

\graphicspath{{figures/}}
\setlength{\textfloatsep}{7pt plus 1pt minus 2pt}
\setlength{\floatsep}{6pt plus 1pt minus 2pt}
\setlength{\intextsep}{6pt plus 1pt minus 2pt}
\setlength{\columnsep}{0.2in}

\newcommand{\figref}[1]{Fig.~\ref{#1}}
\newcommand{\tabref}[1]{Table~\ref{#1}}
\newcommand{\secref}[1]{Section~\ref{#1}}
\newcommand{\floorval}{10^{-1.2}}

\title{Approximating SHARP Doppler Maps from Raw Wi-Fi CSI:\\
Direct Neural Regression versus Phase Correction}

\author{
\IEEEauthorblockN{Gianmaria Frigo}
\IEEEauthorblockA{Department of Information Engineering\\
University of Padova, Italy}
}

\begin{document}

\maketitle

\begin{abstract}
Commodity Wi-Fi channel state information (CSI) enables privacy-preserving human activity recognition, but robust Doppler maps require careful phase sanitization.
We study whether an end-to-end network can replace the optimization-intensive front end of SHARP by regressing its Doppler maps directly from short raw-CSI windows.
We build a reproducible teacher--student pipeline and progressively correct antenna coupling, subcarrier processing, temporal alignment, spectral resolution, motion imbalance, and loss design.
The final 1.314-million-parameter model uses shared antenna weights, a frequency--time encoder, and a full-resolution two-dimensional decoder.
Reconstruction fidelity is evaluated separately from task utility with a frozen SHARP classifier.
Models using either 30 or 242 subcarriers memorize 16 windows to approximately \(4\times10^{-5}\) MSE, yet full-data students collapse toward a smooth center-spectrum shortcut.
On held-out data, the best neural model reaches only \(40.89\%\) classifier accuracy, versus \(91.36\%\) for legacy SHARP.
Conversely, a non-learned affine phase correction followed by the fixed Doppler transform reaches \(91.52\%\), while recomputed SHARP targets themselves reach \(36.86\%\) under the legacy classifier.
Thus, decoder capacity and pixel losses do not resolve the raw-CSI phase nuisance, and map metrics can misrank task utility.
The results motivate hybrid models that learn after explicit phase sanitization rather than replacing it.
\end{abstract}

\begin{IEEEkeywords}
Wi-Fi sensing, channel state information, Doppler, human activity recognition,
neural regression, phase sanitization
\end{IEEEkeywords}

\input{intro}
\input{related}
\input{model}
\input{results}
\input{conclusions}

\IEEEtriggeratref{3}
\bibliographystyle{IEEEtran}
\bibliography{biblio}

\end{document}
